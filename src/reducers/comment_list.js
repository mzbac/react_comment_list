import { ADD_COMMENT, DELETE_COMMENT } from '../actions/types';

export default function(state = [], action) {
  switch (action.type) {
    case ADD_COMMENT:
     
      return state
    case DELETE_COMMENT:
    
      return state
  }

  return state;
}
